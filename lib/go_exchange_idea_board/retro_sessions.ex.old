defmodule GoExchangeIdeaBoard.RetroSessions do
  alias GoExchangeIdeaBoard.{Repo, RetroSession}

  def get_retro_session_by_id(id) do
    case Repo.get_by(RetroSession, id: id) do
      nil ->
        {:error, :not_found}

      retro_session ->
        {:ok, retro_session}
    end
  end
end
